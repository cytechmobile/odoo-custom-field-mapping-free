<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_lead_owner_id_form" model="ir.ui.view">
        <field name="name">lead.owner.id.form</field>
        <field name="model">lead.owner.id</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_lead_owner_id_tree" model="ir.ui.view">
        <field name="name">lead.owner.id.tree</field>
        <field name="model">lead.owner.id</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="action_lead_owner_id" model="ir.actions.act_window">
        <field name="name">Lead Owner Id</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">lead.owner.id</field>
        <field name="view_mode">tree,form</field>
    </record>

</odoo>